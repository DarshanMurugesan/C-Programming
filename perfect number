#include<stdio.h>
void main()
{
  int n,sum=0,temp;
  printf("Enter number to be checked:");
  scanf("%d",&n);
  temp=n;
  for(int i=1;i<=n/2;i++)
  {
      if(n%i==0)
      {
          sum+=i;
      }
  }
  if(sum==temp)
  {
      printf("It is a perfect number.");
  }
}
