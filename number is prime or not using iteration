#include<stdio.h>
int main()
{
  int num,i,isprime=1;
  printf("Enter a positive integer:");
  scanf("%d",&num);
  if(num<2)
  {
      printf("%d is not a prime number\n",num);
      return 0;
  }
}

